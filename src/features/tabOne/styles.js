
import {
  ScaledSheet,
  moderateScale,
  verticalScale,
  scale
} from "react-native-size-matters";
const styles = ScaledSheet.create({
  dashboard_Wrapper: {
    flex: 1
  }
});

export default styles;
